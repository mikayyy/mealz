{
  "name": "mealz",
  "version": "0.21.1",
  "private": true,
  "description": "mealz AI meal planning prototype",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.js",
    "test:browser": "node --test tests/browser/*.test.js",
    "release:check": "node scripts/release-check.js",
    "migration:validate": "node scripts/migrate-validate.js"
  },
  "engines": {
    "node": "22.x"
  },
  "devDependencies": {
    "@electric-sql/pglite": "0.5.8",
    "@types/node": "22.20.4",
    "playwright": "1.63.0",
    "typescript": "7.0.2"
  }
}
